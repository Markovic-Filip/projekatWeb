new Vue({
    el: '#index-app',
    /*
    mounted()    {
        if (window.localStorage.getItem('jwt') != null) {
            // ako je ulogovan da odmah prebaci korisnika na njegovu stranicu umesto index (koji sluzi za neulogovane korisnike)
        }
    },  */
    methods:    {
    /*  Zasad nema potrebe za ovim, izmenio sam potrebu index stranice

        ulogovan: function()    {
            let jwt = window.localStorage.getItem('jwt');
            if (jwt != null)    {
                return true;
            } else    {
                return false;
            }
        }
    */
    }
});
